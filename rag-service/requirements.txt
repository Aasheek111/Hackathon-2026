# Web
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9        # required for form-data uploads
python-dotenv>=1.0

# RAG
numpy<2,>=1.26.2               # pinned - langchain-community and faiss-cpu disagree otherwise
langchain>=0.3,<0.4
langchain-community>=0.3,<0.4  # FAISS vector store lives here
langchain-text-splitters>=0.3,<0.4
faiss-cpu>=1.8                 # no wheels for Python 3.13+ yet - use 3.11 or 3.12
fastembed>=0.4,<1.0            # LOCAL embeddings (ONNX) - no API key, no quota, replaces Gemini embeddings
requests>=2.31,<3.0            # Groq (text/TTS) + Unsplash (visuals)

# Files
pypdf>=4.2
python-docx>=1.1               # teacher uploads in Word format
pillow>=10.3

# Background job queue (full-curriculum generation, YouTube quiz, TTS)
celery>=5.3,<6.0
redis>=5.0,<6.0

# YouTube transcript fetch for the YouTube-quiz feature - free, no API key
# (talks directly to YouTube's own public caption endpoint), replacing an
# earlier SerpApi-based implementation that was both a paid/metered
# dependency and hit a live DNS failure during testing.
youtube-transcript-api>=1.0,<2.0
